const namespace = 'tags';

export const ActionTypes = {
	TagAdd: `${namespace}.tag.add`,
	TagError: `${namespace}.tag.error`,
	TagLoad: `${namespace}.tag.load`,
	TagLoading: `${namespace}.tag.loading`,
	TagSelect: `${namespace}.tag.select`,
};
